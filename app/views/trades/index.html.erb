<!-- <p>Find me in app/views/trades/index.html.erb</p> -->
<table class="table table-bordered" style="width: auto;" align = center>
<h1 align = center>Listing Trades</h1>
 <tr>
            <th>Year</th>
            <th>Exporter</th>
            <th>Importer</th>
            <th>Commodity</th>
            <th>Volume (USD)</th>
          </tr>
<% @trades.each do |trade| %>
<tr>
<td><%= trade.year %> </td>
<td><%= Country.find(trade.exporter_code).name %> </td>
<td><%= Country.find(trade.importer_code).name %> </td>
<td><%= Commodity.find(trade.commodity_code).name.truncate(50, separator: /\s/)%></td>
<td><%= trade.volume %></td>
</tr>
<% end %> 
</table>